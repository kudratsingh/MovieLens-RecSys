{
  "clientId": "movielens-api",
  "name": "MovieLens API (backend)",
  "description": "Confidential client the FastAPI service validates access tokens against. It issues no tokens of its own: password grant and service account are both off, so it exists only as the audience every other client's tokens are minted for.",
  "enabled": true,
  "protocol": "openid-connect",
  "publicClient": false,
  "bearerOnly": false,
  "standardFlowEnabled": true,
  "directAccessGrantsEnabled": false,
  "serviceAccountsEnabled": false,
  "clientAuthenticatorType": "client-secret",
  "redirectUris": ["${APP_ORIGIN}/api/auth/callback/keycloak"],
  "webOrigins": ["${APP_ORIGIN}"],
  "attributes": {
    "access.token.lifespan": "300"
  }
}
