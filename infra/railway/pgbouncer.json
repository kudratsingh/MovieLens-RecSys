{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "infra/pgbouncer/Dockerfile",
    "watchPatterns": [
      "infra/pgbouncer/**"
    ]
  },
  "deploy": {
    "restartPolicyType": "ALWAYS"
  }
}
