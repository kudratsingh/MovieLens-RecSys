# Handoff — Megatron (all lanes)

Update at the END of every turn, before the final message.

- **Current queue position:** M2 and M4 are complete and signaled. M5/W10 is blocked until PR #178 is merged into `origin/main`; fetched main remains `0933834` and does not contain `cd6017d`. M6–M9 have not started this resumed session.
- **M2 / PR #178:** persistent worktree `/Users/kudratsingh/worktrees/MovieRecSys-catalog`, branch `feat/demo-full-catalog`, pushed commit `cd6017d`. Page-shaped load setup now snapshots state through a bounded `sort=popular` walk, reaching the reviewed persona titles before the 62k untouched tail while retaining the strict pool assertion. Syntax plus 20 focused tests pass. Orchestrator owns CI/merge; no auto-merge.
- **M4 / PR #183:** persistent worktree `/Users/kudratsingh/worktrees/MovieRecSys-sasrec-allpositions`, branch `feat/sasrec-all-positions`, pushed at `6fe0d86`. Implements canonical all-position windows, strict timestamp groups, deterministic per-position negatives, L=200 bounded storage tests, torch exact training evaluation/lazy FAISS, objective-versioned artifacts, frozen 6%/full configs, results and ADR 0016 note. Black/Ruff/strict mypy pass; 103 focused tests pass, 1 skipped. Full unit collection is locally blocked only by absent optional `psycopg` and `fakeredis`.
- **M4 valid 6% record:** `f837955c832440069dd8c1316a2ad0c6`, protocol `sha256:090985d…`, warm recall/NDCG 0.182165/0.067743, 80.9 s fit, model SHA `6dfe8d4…`. Versus copied-prefix v1 warm recall: -42.82%.
- **M4 valid full record:** `fd2ee9f6f6794449a31ea3f50e600a48`, protocol `sha256:b4ed5afa…`, warm 0.485648/0.172780, cold 0.526273/0.435841, overall 0.496570/0.243500, 1,931 warm / 710 cold. Versus stable v1: warm recall -4.62%, warm NDCG -9.61%, cold bit-identical. Fit 1,797.4 s (9.8x faster), user CPU 1,795.14 s, max RSS 7.22 GB; 12 h shell elapsed includes host sleep. Model SHA `16631300…`, vocabulary `76f0cf89…`; local and MLflow copies byte-identical and reload/scored-output equal.
- **Retained non-verdict attempts:** Docker-backed pilot `833812ee…` failed only when host upload targeted container-local read-only `/mlartifacts`; its local model/log remain. Full preflight `b3435b73…` was stopped before epoch one for missing ignored cohort payload, tagged with cause and marked `KILLED`; log remains. Machine-readable record: `docs/experiments/sasrec/all-positions-training-2026-09-11.json` in PR #183.
- **ADR 0020 / PR #179:** persistent worktree `/Users/kudratsingh/worktrees/MovieRecSys-adr0020`, pushed `7e2b590`. It records that proposed stop rules 1 and 2 fired, so no 0b/A/B/C/D/E capacity cell is authorized as written. O-22 is open in `DECISIONS.md`; recommended path is P1-only exact copied-prefix memory optimization, retaining P2 as a named ablation.
- **M5 preparation:** exact Starscream sequence re-read. After #178 merges: fresh main worktree; create external `DEMO_COMPOSE_EXTRA` with read-only served-bundle bind and Compose `ports: !override` 55432 remap; full-catalog demo seed; fresh incumbent gate; demo-only promotion with explicit `POSTGRES_PORT=55432`; smoke; unchanged SASRec k6 gate; revert; record both with host-memory evidence and demo caveat; then stop for orchestrator W11.
- **Machine:** no 25M/training process active. Dev Docker stack is up (Postgres, MLflow, Redis, Keycloak, Grafana, Prometheus, PgBouncer) with persistent volumes intact. Do not stop it casually. No demo stack measurement has started.
- **Durable WIP for later:** `feat/synth-cold-v2` at `8d45e82` (M6 incomplete); `feat/twotower-run-artifact` at `339ff60` (older WIP). Preserve all run data and logs.
- **Next action:** wait for orchestrator/CI to merge PR #178; fetch main; execute M5 exactly. Separately await O-22 before any ADR 0020 capacity run. If #178 remains unmerged, do not bypass the prerequisite by measuring its branch.
- **Last updated:** 2026-09-11 10:23 PDT by Megatron.
