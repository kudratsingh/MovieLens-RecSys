# Current state — maintained by the orchestrator

**Updated 2026-09-05 16:40.** **O-13: #166 merged — trainers filter to the default tenant, offline runs unblocked; the 515 stray demo rows remain for the owner to DELETE as hygiene.** `origin/main` past #162. Champions: item-item (warm recall@500 0.3991),
LightGBM (overall NDCG@10 0.1977).

| Line | Status | Owner |
|---|---|---|
| SASRec retrieval | Gate read `incomplete` at evidence recovery, then `promote` once #139 (single-seed regime) and #143 (warm bootstrap band) matched the tool to the policy: warm +16.57%, LB +12.88%, cold 0.00%, overall +11.16%. Artifact `a11af5ed…`, SHA `43320b87…`. Encoder p99 0.285 ms. | Megatron |
| SASRec end-to-end | Fixed ranker on SASRec candidates: warm NDCG@10 +1.67%, cold 0.00%, overall +0.43%. **Passes D-002 non-regression.** Ranker not yet retrained on SASRec candidates. Verdict branch `docs/sasrec-single-run-verdict` being corrected, not merged as written. | Megatron |
| Step 1: retrain LightGBM on SASRec candidates | **Done 2026-09-05 08:20 (Opus failover B), 11 min per run.** Single retrained booster: warm NDCG@10 0.0728→0.0917 (+25.96%), cold −53% (booster misranks the popularity slate), gate refused. **1b per-route bundle: overall 0.2146 (+6.88%), warm +25.96%, cold identical → `promote`. 1c union booster: +6.30% / +26.69% / −1.05% → `promote`.** PR #151 open, CI running, owner merges. First learned two-stage system to clear the end-to-end gate. **O-7 decided: per-route boosters.** M2 (Starscream) now targets this bundle; champion swap after M2's gates. | Opus failover B, done |
| Verdict PR | #150 merged: SASRec "retrieval promotion eligible; end to end blocked on the ranker", D-002 passed. | Opus failover A, done |
| Rung 3 | ADR 0018 **proposed**, PR #152 (docs only, owner approves). Increment 1 = SASRec score + logit as point-in-time LightGBM features, computed in the sidecar not stored; incumbent = per-route bundle `566f5309…`; overall +3% needs warm +9.6%. Increment 2 = DIN only on a named gap. | Opus failover C, done |
| Two-tower v1/v2 | Below popularity 4-10x. To be relabelled "unexplained", not evidence. No reruns. | Starscream (docs only) |
| Content cold-item (ADR 0017) | Coverage yes, relevance 0.0001. Owner decision on TMDB increment 2 pending. | — |
| Noise study M0-14 | Seeds 7/13/21 at 6%: warm 0.3103/0.3258/0.2957, range 9.71%; cold sd exactly 0. Study refused (seed overlap + configuration_id collision). Recorded as raw measurement, not certified. Extra seeds 23/29 killed. | Starscream — on protocol; fixing `_configuration_id()` under lane exception, then M0-14 PR, then M2 |
| M2 serving of SASRec | Not started. Manifest v2, generic retriever interface, sidecar load, k6. | Starscream |
| Deployment | Hetzner box not provisioned. Deferred behind modeling. | — |

## Work left — the whole board, 2026-09-05 evening (orchestrator)

| # | Item | Lane / who | Depends on | Done when |
|---|---|---|---|---|
| W1 | Merge #152 (ADR 0018 accepted, Rung 3 `approved`) after conflict resolve | failover D | — | on main |
| W2 | **Rung 3 increment 1 — final (post-O-9 re-measure, run `bc89411b…`):** learned-route warm NDCG@10 +2.78% vs +3.00% → **refused**; all-routes +0.93%; cold bit-identical. Eight-column control isolates the two SASRec features at **+1.86%**, retrain +0.90%. Features take 82% of gain and popularity collapses: the encoder's scalar verdict on its own candidates adds little to the ranker (ADR 0018 Risk 1). **PR #159 merged 2026-09-06.** Rung 3 closed as measured. | failover D, done | — | done |
| W3 | ADR 0001 amendment + `--scope learned-route` gate mode (O-1) | Megatron | — | PR merged; W2's verdict re-read under it |
| W4 | Two-tower diagnostic (tiny-overfit, cross-path); relabel rows | Megatron | — | PR merged; rows say "defect: …" or "unexplained" |
| W5 | TMDB metadata pull + Postgres load + coverage doc (O-2) | failover E | key present | PR open, DVC snapshot, coverage numbers for ADR 0017 inc. 2 |
| W6 | Manifest v2 — **merged via #161 (stacked); #154 may be redundant** | Starscream | — | done |
| W7 | Generic retriever interface — **merged via #161; #156 may be redundant**. Caught and fixed newest-first vs oldest-first history order. | Starscream | — | done |
| W8 | **Sidecar loads the per-route SASRec bundle — PR #161 merged 2026-09-06 00:19Z**, probe-authoritative fast-path guard, fixtures at 1/3/12/49/50, cold bit-identical fixture. | Starscream | — | done |
| W9 | Prediction audit fields (retriever family, artifact SHA, route, encoder ms); isolated encoder benchmark in the sidecar | Starscream | W8 | PR merged; p99 < 15 ms |
| W10 | Unchanged k6 gate with the SASRec bundle serving | Starscream | W9 | p99 < 100 ms, zero correctness errors, evidence dir |
| W11 | Champion swap to the per-route SASRec bundle, item-item kept as rollback | orchestrator (owner-authorised) | W8–W10 green | dated notes on ADR 0016 + roadmap; `make prod-verify` equivalent passes |
| W12 | Housekeeping PR: CLAUDE.md status → today, status ledger, two-tower rows, archive docs/progress.md, sasrec_latency path fix, prune merged worktrees | Starscream | W4 for the two-tower wording | PR merged |
| W13 | Rung 3 increment 2 (DIN) — **not built; stop rule confirmed after the O-9 re-measure (+2.78% < +3%).** Rung 3 closes as measured. | closed | — | — |
| W14 | ADR 0017 increment 2: content retriever on TMDB text (cold items) — proposal only | not started | W5 | ADR note proposed |
| W15 | Next sequence-model proposal (wider SASRec / longer seq / sampled softmax) — reopens O-3 GPU | not started | W2 | ADR proposal |
| W16 | Hetzner deploy (runbook §1–§7) — still owed, owner's hands (DNS, box, secrets) | owner | — | DEPLOY-OK |

Rules for everyone: one job on the 25M at a time (check `pgrep -f src.training` before launching; report before launching); merged PRs are pruned from worktrees by their owner; nothing changes a gate threshold.
| W17 | **SASRec NaN fix, PR #162 (Megatron), merged.** 131 of 1,931 warm users had empty learned-route slates. Corrected: warm recall@500 **0.4652 → 0.5092** (+27.6% vs item-item 0.3991), retrieval gate promotes; per-route bundle warm NDCG@10 **0.0917 → 0.1014**, overall **0.2146 → 0.2218** (+10.4% vs item-item+LightGBM 0.2008), both gate scopes promote. | Megatron, done | — | done |
| W18 | **Two-tower defect fixed (PR #158, merged)**: FAISS row→dense-id off-by-one. Corrected 6% pilot warm recall@500 **0.3759** vs item-item 0.3587 on the same subsample. Rung 1 reopened. | Megatron | — | merged on green |
| W19 | **DONE 2026-09-06 01:09 (failover F), PR #182.** Corrected two-tower v2 full data: warm recall@500 **0.5113** (+28.14% vs item-item, LB +25.28%), cold identical, overall +18.95% → retrieval gate **PROMOTE**. Within 0.4% of SASRec (0.5092). Retrieval line decision (SASRec vs two-tower vs union, ADR 0019) is the owner's. | failover F, done | — | merging |
| W21 | Model-side items for serving: publish the popularity fallback in the SASRec bundle; move `routing.py` out of the implicit-importing package. | Megatron | W17 | PR merged |
| W10a | **Incumbent k6 baseline measured (Starscream, main 8863157):** p50 7.21 / p95 9.98 / p99 12.93 ms vs <100; 3,301 req, 0 errors, 0 breaches, steal 0, Postgres on real disk (fdatasync in the number), host port remapped 55432 via DEMO_COMPOSE_EXTRA. Evidence held for the W10 PR as the control. | Starscream | — | done |
| W22 | Re-run the incumbent k6 baseline on a main that includes #168 (four audit columns per request are inside the p99); report both numbers, pre-#168 kept as the cost of the columns. SASRec measured with encoder hooks in place. | Starscream | #168 merged | both baselines in the W10 PR |
| W23 | Execute a real `alembic downgrade` across 0019 (and 0018) in the staging compose before the first production deploy; today the reverse path is only structurally tested. | Starscream | #168 merged | rollback rehearsal recorded |
| W24 | Product track: surface `retriever_family`, `retriever_sha256`, `ranker_route`, `encoder_ms` in the frontend prediction-audit disclosure after the champion swap. | frontend lane, later | W11 | — |
| W25 | **New dependency (PR #169):** production compose points `MODEL_ARTIFACT_DIR` at `/app/models/served-bundle`, which is empty until the served payload is published; a production container refuses to boot until then (fail-closed by design). First production deploy (W16) is therefore gated on the served payload, which is gated on Megatron W21 (popularity artifact) → publish → commit. | Starscream after W21 | W21 | `infra/model-bundle-served/` holds the published bundle; CLAUDE.md tree lists the directory (one-line follow-up) |
| W26 | **OpenMP collision is not fixed by `OMP_NUM_THREADS=1`** (F, 2026-09-05 22:06): it moves the abort from torch to the first faiss index build (`OMP Error #15`). `KMP_DUPLICATE_LIB_OK=TRUE` clears it; being validated by reproducing the 6% two-tower pilot exactly. Clean fix: exact search via torch matmul + top-k for training-time evaluation (drop faiss from the trainer), or a single-libomp venv. | Megatron / F | after W19 | PR + docs/results.md caveat corrected |
| W27 | **PR #178 open, CI RED: synthetic-load-smoke DiskFull on the CI tmpfs Postgres during demo-materialize (499k user_item_features rows). Fix tmpfs size or materialization footprint first.** Original problem: W10 cannot run on the 120-item demo catalog: SASRec retrieves from 34,461 items, `hydrate_ranked_movies` finds none in the demo `movies` table, the API fails closed to popularity. Ruling: full-catalog demo tenant (all 62,423 movies, item features materialized for all, user features for the 8 personas; user×item affinity is 8×62k rows, so D-009 does not bite here), then fresh baseline + W10 on the same tenant. `make promote` must print its target DB and refuse an unset POSTGRES_PORT on a demo stack (near-miss against the dev DB). | Starscream | — | W10 number on the record; champion swap decision follows |
| W28 | **Rewrite the SASRec training loop** to slice windows and predict all positions per window (canonical SASRec) instead of one full forward per target; memory bounded for L=200. Prerequisite for any ADR 0020 cell (35–58 days → 17–29 h on CPU). Equality fixture vs the current builder on a small corpus; one 6% pilot to confirm the number reproduces or state why not. | training lane | ADR 0020 approval | PR merged; results.md note |
| M10 | **Dependabot: GitHub now reports 11 alerts on main (4 critical, 2 high, 4 moderate, 1 low).** Triage `/security/dependabot`, bump the affected pins (web/ and Python), run the full CI, one PR. Do this before M5 if any critical alert touches the serving images. | Megatron | — | 0 open critical/high |
